# 🎮 n8n Gaming Templates - Полное руководство

Автоматический выбор визуальных стилей для Shorts с поддержкой **кириллицы** и **фильтрацией по тематике**.

---

## 📦 Что здесь?

### Файлы:
- **[n8n-workflow.json](n8n-workflow.json)** - готовый n8n workflow (импортируй и используй)
- **[TEMPLATES_GUIDE.md](TEMPLATES_GUIDE.md)** - подробное описание всех 30 шаблонов
- **[templates-library.js](templates-library.js)** - исходный код библиотеки шаблонов (для разработки)

---

## 🚀 Быстрый старт

### 1. Импортируй workflow в n8n

1. Открой n8n
2. **"+"** → **"Import from File"**
3. Выбери **`n8n-workflow.json`**
4. Замени `YOUR_API_KEY` на свой ключ в node "Video Processor API"

### 2. Подключи к LLM

Workflow ожидает данные от LLM в формате:

```json
{
  "shorts": {
    "title": "Эпичный момент!",
    "start": 10.5,
    "end": 70.2,
    "subtitles": [
      {"text": "Привет всем", "start": 0.0, "end": 1.5},
      {"text": "это крутой момент", "start": 1.5, "end": 3.2}
    ],
    "client_meta": {}
  }
}
```

**Готово!** Случайный шаблон выберется автоматически из всех 30.

---

## 🎯 Фильтрация по тематике

Workflow поддерживает **3 способа фильтрации** шаблонов через поле `client_meta`:

### 1️⃣ По категории стиля

Выбери одну из трёх категорий оформления:

```json
{
  "client_meta": {
    "template_category": "GAMING_CONTRAST"
  }
}
```

**Доступные категории:**

| Категория | Шаблонов | Описание | Когда использовать |
|-----------|----------|----------|-------------------|
| `GAMING_CONTRAST` | 10 | Заголовок на плашке,<br>субтитры БЕЗ плашки | Нужен акцент на заголовок,<br>важная информация в title |
| `DOUBLE_BOX` | 10 | Заголовок И субтитры<br>на плашках | Сложный фон видео,<br>максимальная читаемость |
| `NO_BOX` | 10 | Только обводка,<br>БЕЗ плашек | Мемы, вирусный контент,<br>чистый фон |

### 2️⃣ По жанру игры

Workflow автоматически выберет подходящие стили для жанра:

```json
{
  "client_meta": {
    "template_genre": "cyberpunk"
  }
}
```

**Доступные жанры и подходящие стили:**

| Жанр | Примеры игр | Стили |
|------|-------------|-------|
| `cyberpunk`, `tech`, `sci-fi` | Cyberpunk 2077 | Cyber Neon, Matrix Code, Double Cyber |
| `action`, `battle`, `pvp` | Call of Duty, Apex | Fire & Ice, Double Fire, Outline Fire |
| `rpg`, `fantasy`, `magic` | Elden Ring, Skyrim | Gold & Purple, Double Purple, Outline Purple |
| `horror`, `zombie`, `survival` | Resident Evil | Toxic Green, Blood Shadow, Outline Blood |
| `racing`, `speed`, `energy` | Forza, NFS | Electric Yellow, Outline Fire |
| `strategy`, `empire`, `rts` | Civilization, AOE | Royal Blue, Double Clean |
| `boss`, `epic` | Любые boss fights | Lava Glow, Double Gold, Outline Gold |
| `space`, `cosmic`, `alien` | No Man's Sky | Cosmic Purple, Double Ice |
| `frost`, `winter`, `frozen` | Elden Ring (зима) | Double Ice, Outline Ice |
| `poison`, `radioactive`, `acid` | Fallout | Double Toxic, Outline Toxic |
| `meme`, `viral`, `fun` | Любые | Outline Classic, Outline Rainbow |

### 3️⃣ Точный выбор шаблона

Укажи конкретный шаблон по имени:

```json
{
  "client_meta": {
    "template_name": "cyber_neon"
  }
}
```

**Список всех 30 шаблонов:** см. [TEMPLATES_GUIDE.md](TEMPLATES_GUIDE.md)

### 4️⃣ Комбинация фильтров

Можно комбинировать категорию + жанр для точного выбора:

```json
{
  "client_meta": {
    "template_category": "GAMING_CONTRAST",
    "template_genre": "rpg"
  }
}
```

**Как работает фильтрация:**
1. Сначала фильтрация по `template_category` (если указана)
2. Затем фильтрация по `template_genre` (если указан)
3. Из отфильтрованных выбирается случайный
4. Если фильтры не указаны - выбирается из всех 30

---

## 🎨 Примеры использования

### Для разных типов контента:

**Вирусные моменты / Мемы:**
```json
{"client_meta": {"template_category": "NO_BOX"}}
```

**Важные гайды / Туториалы:**
```json
{"client_meta": {"template_category": "DOUBLE_BOX", "template_genre": "important"}}
```

**Boss fights / Эпичные моменты:**
```json
{"client_meta": {"template_genre": "boss"}}
```

**Хорроры / Страшилки:**
```json
{"client_meta": {"template_genre": "horror"}}
```

**RPG / Fantasy:**
```json
{"client_meta": {"template_genre": "rpg"}}
```

---

## ✨ Особенности

✅ **30 gaming шаблонов** с полной поддержкой кириллицы
✅ **Фильтрация по категории** - выбор стиля оформления
✅ **Фильтрация по жанру** - автоподбор под тип игры
✅ **Точный выбор** шаблона по имени
✅ **Случайный выбор** - разнообразие контента
✅ **Шрифты с кириллицей:**
  - **Russo One** - gaming display font
  - **Montserrat** - универсальный, 9 весов
  - **PT Sans** - оптимизирован для русского
  - **Oswald** - condensed font
  - **Fixel** - геометрический, 18 вариантов

---

## 🔧 Настройка

### Изменить URL API

В node "Video Processor API" → URL:
- **Docker Compose:** `http://video-processor:5001/process_video`
- **Публичный сервер:** `https://yourdomain.com/process_video`

### Тестовые данные

Нажми на node "When clicking 'Test workflow'" и используй:

```json
{
  "source_video_url": "https://test-videos.co.uk/vids/bigbuckbunny/mp4/h264/360/Big_Buck_Bunny_360_10s_1MB.mp4",
  "shorts": {
    "title": "Тестовое видео",
    "start": 0,
    "end": 10,
    "subtitles": [
      {"text": "Привет", "start": 0.0, "end": 1.5},
      {"text": "это тест", "start": 1.5, "end": 3.0},
      {"text": "gaming шаблонов", "start": 3.0, "end": 5.0}
    ],
    "client_meta": {
      "template_genre": "cyberpunk"
    }
  }
}
```

### Всегда использовать один шаблон

В Code Node найди строку:
```javascript
const [templateKey, tpl] = filteredTemplates[Math.floor(Math.random() * filteredTemplates.length)];
```

Замени на:
```javascript
const [templateKey, tpl] = filteredTemplates[0]; // Всегда первый из отфильтрованных
```

---

## 📊 Метаданные в ответе

После обработки `client_meta` обогащается информацией о выбранном шаблоне:

```json
{
  "task_id": "abc-123",
  "status": "processing",
  "client_meta": {
    "_template_key": "cyber_neon",
    "_template_name": "Cyber Neon",
    "_template_category": "GAMING_CONTRAST",
    "_template_genres": ["cyberpunk", "tech", "sci-fi"],
    "_templates_available": 3
  }
}
```

**Поля:**
- `_template_key` - ключ шаблона (для повторного использования)
- `_template_name` - красивое название
- `_template_category` - категория шаблона
- `_template_genres` - список подходящих жанров
- `_templates_available` - сколько шаблонов подошло под фильтры

---

## 🔍 Отладка

### Проверить какой шаблон выбран

Посмотри поле `_template_name` в `client_meta` ответа.

### Если подошло мало шаблонов

Проверь `_templates_available`:
- **0** - фильтры слишком строгие или ошибка в названии жанра
- **1** - нашёлся только один шаблон
- **3+** - хорошее разнообразие

### Если нужна максимальная вариативность

Не передавай фильтры вообще - будут использованы все 30 шаблонов.

---

## 📚 Дополнительная документация

- **[TEMPLATES_GUIDE.md](TEMPLATES_GUIDE.md)** - подробное описание всех 30 шаблонов с цветовыми схемами
- **[templates-library.js](templates-library.js)** - исходный код для кастомизации

---

## 🆕 История версий

- **v3.0 Gaming Edition** (текущая) - 30 gaming шаблонов с фильтрацией по тематике
- **v2.0** - 20 универсальных шаблонов с категориями
- **v1.0** - 5 базовых шаблонов

---

## 💡 Best Practices

### 1. Для вирусного контента
Используй **NO_BOX** - классический мем-стиль привлекает внимание:
```json
{"client_meta": {"template_category": "NO_BOX"}}
```

### 2. Для образовательного контента
Используй **DOUBLE_BOX** - максимальная читаемость:
```json
{"client_meta": {"template_category": "DOUBLE_BOX"}}
```

### 3. Для динамичного геймплея
Используй **GAMING_CONTRAST** - акцент на действие:
```json
{"client_meta": {"template_category": "GAMING_CONTRAST"}}
```

### 4. Автоматический выбор под игру
Передавай только жанр - workflow сам выберет лучший стиль:
```json
{"client_meta": {"template_genre": "action"}}
```

### 5. Интеграция с LLM
Добавь в промпт LLM:
```
Определи жанр игры из видео (cyberpunk, rpg, horror, action, racing, etc.)
и добавь в client_meta:
{
  "template_genre": "<определённый_жанр>"
}
```

---

**Версия:** 3.0 Gaming Edition
**Дата:** 2025-11-16
**Шаблонов:** 30
**Категорий:** 3
**Жанров:** 20+
**Шрифты:** Russo One, Montserrat, PT Sans, Oswald, Fixel (все с кириллицей)
