services:
  video-processor:
    # For local testing: build from source
    build:
      context: /Users/bic/dev/video-processor-api
      dockerfile: Dockerfile
    image: video-processor-api-local:test
    volumes:
      # Map tasks directory to testing folder to avoid committing test files
      - ./testing/tasks:/app/tasks
